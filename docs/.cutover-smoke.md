# Cutover smoke test

Throwaway. This PR exists to prove the GitHub App path end to end after
Task 10: webhook delivery -> durable job -> claim -> reader -> neutral
check run on the PR itself.

One real line rather than the plan's empty commit, deliberately: an empty
diff gives the reader zero changed files, and a smoke test that fails on a
degenerate input tells you nothing about the thing you are testing.

Close without merging and delete the branch.
