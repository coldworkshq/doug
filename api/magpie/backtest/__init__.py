"""Phase 0: the backtest.

The thesis's falsifiable claim — cheap metadata features capture a
disproportionate share of defect-inducing changes — gets measured here,
over real history, before anything else gets built. The replay calls the
same extract/score functions the live webhook will use.
"""
